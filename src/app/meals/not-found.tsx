const NotFound = () => (
  <main className="error">
    <h1 className="highlightedText">Meal Found</h1>
    <p>Unfortunately, we could not find the requested page or resource.</p>
  </main>
);

export default NotFound;
